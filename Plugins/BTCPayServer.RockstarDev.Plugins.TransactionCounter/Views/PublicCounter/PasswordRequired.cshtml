@using BTCPayServer.RockstarDev.Plugins.TransactionCounter.ViewModels
@model BaseCounterPublicViewModel
@{
	Layout = "_CounterPublicLayout";
	var currentPath = Context.Request.Path;
}
<div class="container d-flex align-items-center justify-content-center" style="min-height: 80vh;">
	<div class="col-lg-5 col-md-7 col-sm-9">
		<div class="text-center mb-4">
			<h1 class="display-5 mb-3">
				<span class="text-warning">🔒</span> Password Required
			</h1>
			<p class="text-muted">This page is password protected. Please enter the password to continue.</p>
		</div>

		<div class="card shadow-sm border-0 p-4">
			<form method="get" action="@currentPath">
				<div class="form-group mb-4">
					<div class="input-group">
						<input type="password" class="form-control form-control-lg border-0 bg-light"
							   name="password" placeholder="Enter password" required
							   style="box-shadow: none;">
					</div>
				</div>
				<div class="d-grid">
					<button type="submit" class="btn btn-primary btn-lg">
						Submit
					</button>
				</div>
			</form>
		</div>
	</div>
</div>
